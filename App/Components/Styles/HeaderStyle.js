import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    // flex: 1,
    height: 50
    // backgroundColor: Colors.primary
  },
  text: {
    textAlign: 'center'
  }
})
